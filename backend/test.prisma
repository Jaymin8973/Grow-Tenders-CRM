
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://user:pass@host/db"
}

model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
